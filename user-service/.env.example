# user-service environment
# Copy to .env and adjust for local standalone runs (outside docker compose)

DATABASE_URL=postgresql://user:changeme@localhost:5432/userdb
OTLP_ENDPOINT=http://localhost:4318
