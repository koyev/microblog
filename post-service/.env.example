# post-service environment
# Copy to .env and adjust for local standalone runs (outside docker compose)

DATABASE_URL=postgresql://user:changeme@localhost:5432/postdb
RABBITMQ_URL=amqp://guest:changeme@localhost/
OTLP_ENDPOINT=http://localhost:4317
